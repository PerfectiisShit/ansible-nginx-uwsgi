---
- name: Remove base directory (if present)
  file:
    path: '{{ base_dir }}'
    state: absent

- name: Clone Git repository
  git:
    repo: '{{ git_repo }}'
    dest: '{{ base_dir }}'
    version: '{{ git_branch }}'

- name: Install requirements.txt
  command: "/bin/pip install -r '{{ base_dir }}/requirements.txt'"

- name: Set permissions for base directory
  file:
    path: '{{ appdata_dir }}'
    owner: '{{ uid }}'
    group: '{{ gid }}'
    state: directory
    recurse: yes

- name: create nginx log directory
  file:
    path: /var/log/nginx/{{ app_name }}
    state: directory
